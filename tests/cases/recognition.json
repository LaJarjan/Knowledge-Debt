[
  {"name":"asyncio semaphore","source":"import asyncio\ndef work():\n    gate = asyncio.Semaphore(4)","expected":["concurrency"]},
  {"name":"module alias","source":"import asyncio as aio\ndef work():\n    gate = aio.Semaphore(4)","expected":["concurrency"]},
  {"name":"symbol alias","source":"from asyncio import Semaphore as Gate\ndef work():\n    gate = Gate(4)","expected":["concurrency"]},
  {"name":"bounded threading semaphore","source":"from threading import BoundedSemaphore\ndef work():\n    gate = BoundedSemaphore(4)","expected":["concurrency"]},
  {"name":"unknown symbol","source":"def work():\n    gate = Semaphore(4)","expected":[]},
  {"name":"custom module","source":"import custom\ndef work():\n    gate = custom.Semaphore(4)","expected":[]},
  {"name":"shadowed argument","source":"import asyncio\ndef work(asyncio):\n    gate = asyncio.Semaphore(4)","expected":[]},
  {"name":"reassigned alias","source":"from asyncio import Semaphore as Gate\ndef work():\n    Gate = custom\n    gate = Gate(4)","expected":[]},
  {"name":"local import","source":"def work():\n    from asyncio import Semaphore as Gate\n    gate = Gate(4)","expected":["concurrency"]},
  {"name":"wildcard uncertainty","source":"import asyncio\nfrom unknown import *\ndef work():\n    gate = asyncio.Semaphore(4)","expected":[]},
  {"name":"counted retry","source":"import time\ndef work():\n    for attempt in range(3):\n        try:\n            return request()\n        except TimeoutError:\n            time.sleep(2 ** attempt)","expected":["retry"]},
  {"name":"delay alias","source":"from time import sleep as wait\ndef work():\n    for attempt in range(3):\n        try:\n            return request()\n        except TimeoutError:\n            wait(1)","expected":["retry"]},
  {"name":"ordinary batch","source":"import time\ndef work(items):\n    for item in items:\n        try:\n            process(item)\n        except ValueError:\n            log(item)\n        time.sleep(1)","expected":[]},
  {"name":"delay outside handler","source":"import time\ndef work():\n    for attempt in range(3):\n        try:\n            return request()\n        except TimeoutError:\n            pass\n        time.sleep(1)","expected":[]},
  {"name":"custom sleep","source":"def work():\n    for attempt in range(3):\n        try:\n            return request()\n        except TimeoutError:\n            sleep(1)","expected":[]},
  {"name":"counted batch by index","source":"import time\ndef work():\n    for item in range(3):\n        try:\n            return process(item)\n        except TimeoutError:\n            time.sleep(1)","expected":[]},
  {"name":"no success exit","source":"import time\ndef work():\n    for attempt in range(3):\n        try:\n            request()\n        except TimeoutError:\n            time.sleep(1)","expected":[]},
  {"name":"shadowed range","source":"import time\ndef work(range):\n    for attempt in range(3):\n        try:\n            return request()\n        except TimeoutError:\n            time.sleep(1)","expected":[]},
  {"name":"finally cleanup","source":"def work(conn):\n    try:\n        return conn.read()\n    finally:\n        conn.close()","expected":["cleanup"]},
  {"name":"conditional cleanup still only structural","source":"def work(conn):\n    try:\n        return conn.read()\n    finally:\n        if condition:\n            conn.close()","expected":["cleanup"]},
  {"name":"close without finally","source":"def work(conn):\n    conn.close()","expected":[]},
  {"name":"context manager outside support","source":"def work():\n    with open('x') as stream:\n        return stream.read()","expected":[]},
  {"name":"cleanup in nested closure is not executed cleanup","source":"def work(conn):\n    try:\n        conn.read()\n    finally:\n        def later():\n            conn.close()","expected":[]},
  {"name":"async retry","source":"import asyncio\nasync def work():\n    for attempt in range(3):\n        try:\n            return await request()\n        except TimeoutError:\n            await asyncio.sleep(1)","expected":["retry"]}
]
